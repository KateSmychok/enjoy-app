export enum ButtonType {
  PRIMARY = 'primary',
  PRIMARY_OUTLINED = 'primary_outlined',
  SECONDARY = 'secondary',
  INVISIBLE = 'invisible',
}

export enum ToastType {
  Success = 'success',
  Failed = 'failed',
}
